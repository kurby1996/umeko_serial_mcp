# 2026-08-03

- 10:05 在 Web 面板（dashboard.html）增加「连续同来源省略源头标签」：
  - 连续相同 source 的日志只显示第一条的 `[下位机 返回]` / `[AI Agent 发出]` 等标签；
  - 来源切换时自动恢复显示；
  - 清屏后重置状态；后端缓冲与 TXT 导出保持完整标签不变。
- 10:26 在 Web 面板增加 MobaXterm 风格的关键词彩色高亮：
  - error/fail/fatal/err/exception/crash → 红色 (#ff6b6b)
  - warn/warning/alert/retry/timeout → 黄色 (#fbbf24)
  - success/ok/pass/ready/done/connected/open → 绿色 (#34d399)
  - info/notice/status → 蓝色 (#60a5fa)
  - 在 escapeHtml 之后做正则替换，不破坏 XML 安全。
- 10:31 优化 Web 面板日志区显示：
  - 「日志字号」滑块现在只改变日志内容区字号，不再影响 UI 其他部分；
  - 日志区默认最小高度从 220px 提高到 420px，默认更宽；
  - 高亮关键词（error/warn 等）使用相对 1.05em，随日志字号同步放大；
  - 显示栏新增「只看日志」按钮，点击后隐藏顶部、连接、显示、发送区，仅保留日志区；
  - 全屏日志模式下右上角显示浮动「退出只看日志」按钮，也可按 Esc 退出；
  - 状态保存到 localStorage，刷新后保持；
  - 修复 Bug：logTheaterBtn 声明在 applyPrefs() 调用之后导致 TDZ ReferenceError；
  - 将 logTheaterBtn / theaterExitBtn 移至脚本顶部 DOM 引用区域，确保在 applyPrefs() 之前初始化。
- 10:55 调整 Web 面板工具栏布局：
  - 将「清屏」按钮从连接卡移到显示卡，与「只看日志」并排；
  - 减少连接区占用的一行空间，布局更紧凑；
  - 字号设置仍通过 localStorage（serial_hub_ui_prefs_v2）自动保存。
- 11:00 在全屏日志模式（只看日志）右上角增加「清屏」按钮：
  - 与「退出只看日志」并排显示；
  - 全屏下无需退出即可一键清空日志；
  - 普通模式下不显示该浮动按钮。
- 11:03 在 Web 面板增加「自动隐藏工具栏」功能：
  - 显示栏新增「自动隐藏」复选框，默认关闭；
  - 开启后顶部标题/连接/显示区会像 Windows 任务栏一样自动缩到顶部；
  - 鼠标移回顶部 6px 悬停区时工具栏自动滑下；
  - 鼠标离开后自动收回，日志区获得更大可视空间；
  - 状态保存到 localStorage（serial_hub_ui_prefs_v2）；
  - 「只看日志」全屏模式下该浮动工具栏完全隐藏。
