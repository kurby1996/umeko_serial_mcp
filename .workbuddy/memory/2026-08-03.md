# 2026-08-03

- 10:05 在 Web 面板（dashboard.html）增加「连续同来源省略源头标签」：
  - 连续相同 source 的日志只显示第一条的 `[下位机 返回]` / `[AI Agent 发出]` 等标签；
  - 来源切换时自动恢复显示；
  - 清屏后重置状态；后端缓冲与 TXT 导出保持完整标签不变。
- 10:11 在 Web 面板增加「时间戳」与「源头标签」显示开关：
  - 两个复选框默认勾选；取消勾选后隐藏对应内容；
  - 切换时实时重绘已有日志；
  - 偏好设置保存到 localStorage。
