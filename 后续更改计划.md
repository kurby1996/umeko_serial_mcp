# 串口 MCP 后续更改计划

本文档用于记录 `umeko_serial_mcp` 当前状态、已经完成的修改，以及后续维护任务。后续让 AI 继续开发时，优先阅读本文档和项目根目录的 `README.md`。

## 1. 当前项目位置

```text
F:\AI\串口MCP\umeko_serial_mcp
```

项目来源：<https://github.com/umeiko/umeko_serial_mcp>

当前运行环境：

- Windows
- Python 3.12
- 虚拟环境：`.venv`
- MCP 通过 stdio 启动
- HTTP 监控端口：8080
- WebSocket 监控端口：8081

## 2. 当前已完成

### 2.1 基础运行环境

- 已创建 `.venv`。
- 已安装 `pyserial`、`websockets` 和 MCP SDK。
- 已验证 MCP 服务可以启动。
- 已将 MCP SDK 依赖限制为 `mcp>=1.27.0,<2.0.0`。

原因：当前源码使用 `mcp.server.fastmcp`，直接安装 MCP 2.x 会导致导入失败。

### 2.2 串口监控

- MCP 可以枚举串口。
- MCP 可以连接、断开、读写串口。
- 后台线程持续读取串口。
- 浏览器通过 WebSocket 接收硬件日志。
- AI 通过 `read_data` 读取串口缓冲。
- 网页和 AI 共用同一个串口连接，不重复打开 COM 口。

### 2.3 编码支持

已支持：

- `utf-8`
- `gbk`
- `gb18030`

连接时可以指定：

```text
connect_port(port="COM1", baudrate=115200, encoding="gbk")
```

连接后也可以切换：

```text
set_encoding(encoding="gbk")
```

网页监控面板也增加了编码选择框。

相关代码：

- `src/umeko_serial_mcp/server.py`
- `src/umeko_serial_mcp/dashboard.html`
- `pyproject.toml`
- `uv.lock`

## 3. 当前运行方法

### 3.1 启动 MCP 服务

```powershell
cd "F:\AI\串口MCP\umeko_serial_mcp"
.\.venv\Scripts\start-serial-mcp.exe
```

该命令启动的是 stdio MCP 服务，终端没有普通日志输出是正常的。

### 3.2 Codex 配置

```toml
[mcp_servers.serial-mcp]
command = 'F:\AI\串口MCP\umeko_serial_mcp\.venv\Scripts\start-serial-mcp.exe'
```

修改代码或依赖后，需要重启 Codex，使 MCP 子进程重新加载。

### 3.3 启动网页

让 AI 调用：

```text
start_monitor_ui(http_port=8080)
```

浏览器打开：

<http://127.0.0.1:8080>

网页服务不会自动打开浏览器，需要手动访问链接。

### 3.4 连接中文 STM32 固件

```text
connect_port(port="COM1", baudrate=115200, encoding="gbk")
```

如果已经连接：

```text
set_encoding(encoding="gbk")
```

## 4. 已知问题

### P0：旧 MCP 进程不会自动加载代码修改

修改源码后，Codex 中已经运行的 MCP 子进程仍然使用旧代码。必须重启 Codex 或重新加载 MCP 配置。

### P0：网页服务监听所有网卡

当前 HTTP 和 WebSocket 使用 `0.0.0.0`。这会让局域网设备可能访问串口监控和发送命令。

后续应默认改为 `127.0.0.1`，并提供显式的远程访问开关。

### P1：AI 缓冲区会被读取后清空

当前 `read_data` 会消费并清空 `llm_read_buffer`。这不适合多个 AI 客户端，也不支持按时间或序号重复查询。

建议改成带序号的环形缓冲区，并引入读取游标。

### P1：缺少实时等待工具

当前 AI 只能主动调用 `read_data`。闭环测试需要：

```text
wait_for(pattern="TEST_PASS", timeout_ms=30000)
wait_for(pattern="HardFault|ASSERT", timeout_ms=30000)
```

### P1：日志没有持久化

目前网页刷新后历史日志丢失，也没有滚动日志文件。

### P1：串口配置不完整

后续应支持：

- 数据位
- 停止位
- 校验位
- RTS/CTS
- DTR/RTS 控制
- 自动识别 VID/PID

### P2：二进制数据处理能力不足

当前串口读取主要面向文本。二进制协议应支持 Hex、Base64 和原始字节模式，避免解码丢失信息。

## 5. 推荐后续开发顺序

### 第一步：安全和编码完善

- HTTP/WS 默认绑定 `127.0.0.1`。
- 增加配置项控制监听地址。
- 增加编码状态显示。
- 增加 Hex 原始数据显示。
- 对无效编码返回结构化错误。

### 第二步：环形缓冲区和实时查询

建议每条日志使用以下结构：

```json
{
  "seq": 1842,
  "timestamp": "2026-08-02T14:35:21.183+08:00",
  "direction": "RX",
  "encoding": "gbk",
  "text": "原子哥论坛:www.openedv.com\\r\\n",
  "hex": "D4 AD D7 D3 ..."
}
```

新增 MCP 工具：

- `serial_tail(cursor, limit)`
- `serial_search(pattern, cursor)`
- `serial_stats()`
- `serial_clear()`
- `serial_export(path)`

### 第三步：闭环测试工具

新增：

- `wait_for(pattern, timeout_ms)`
- `send_and_wait(data, pattern, timeout_ms)`
- `run_test_sequence(steps)`

测试步骤至少支持：

- send
- delay
- expect
- capture
- pass/fail

### 第四步：日志文件和网页增强

- 按日期滚动保存日志。
- 限制单个日志文件大小。
- 网页增加清屏、暂停、搜索、过滤、导出。
- 网页显示当前端口、波特率、编码、收发字节数。
- WebSocket 断线后从游标续传，避免丢日志。

### 第五步：自动化测试

至少覆盖：

1. UTF-8 文本接收。
2. GBK 文本接收。
3. GB18030 文本接收。
4. 编码切换后继续接收。
5. WebSocket 广播。
6. 多个网页客户端同时订阅。
7. 串口断开和重连。
8. `wait_for` 超时和成功。
9. 非法编码参数。
10. 二进制 Hex 数据不丢失。

优先使用 COM1/COM2 虚拟串口回环测试；不要在真实设备上自动发送未知命令。

## 6. STM32 调试约定

建议 STM32 固件使用统一命令和日志格式：

```text
PING
INFO
TEST ALL
TEST <suite>
RESET
```

推荐日志使用 JSON Lines：

```json
{"event":"boot","fw":"1.2.0","git":"a31c9e2"}
{"event":"test_start","suite":"motor"}
{"event":"test_pass","suite":"motor","elapsed_ms":832}
```

建议 AI 闭环流程：

1. 编译固件。
2. 烧录固件。
3. 连接串口并选择编码。
4. 等待 `boot`。
5. 发送 `TEST ALL`。
6. 等待 `TEST_PASS` 或错误模式。
7. 失败时收集完整日志。
8. 必要时进入 SWD/GDB Fault 诊断。

## 7. 修改注意事项

- 不要让网页进程和 MCP 进程分别打开同一个 COM 口。
- 所有串口读操作必须由一个后台读取线程完成。
- 网页和 AI 只能订阅共享缓冲或事件流。
- 默认禁止远程网络访问。
- 所有写串口操作都应返回明确的成功或失败结果。
- 修改依赖时同时更新 `pyproject.toml` 和 `uv.lock`。
- 修改后必须重启 MCP 服务并执行最小回归测试。

